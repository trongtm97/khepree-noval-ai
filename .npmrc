# Prefer better-sqlite3 N-API prebuilds; avoid node-gyp when VS Build Tools absent.
build_from_source=false
# npm 10 injects implicit `node-gyp rebuild` for better-sqlite3@13 (npm/cli#9837).
# Use `npm run ci:deps` on clean installs without VS Build Tools; plain `npm ci` needs VS or npm 11+ fix.
