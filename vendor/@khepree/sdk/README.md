# Vendored `@khepree/sdk`

Snapshot of the Khepree desktop SDK types/contracts used by NovelTrans.

**Why:** public CI cannot resolve `file:../KHEPREE/packages/sdk`. This folder is the installable source of truth for this repository.

When the upstream SDK changes, refresh files under `src/` and bump the comment/version if needed.
